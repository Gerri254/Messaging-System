# =============================================================================
# Frontend Production Environment Configuration
# =============================================================================

# Backend API Configuration
# Update with your Railway deployment URL after backend deployment
VITE_API_URL=https://your-app.railway.app/api
VITE_BACKEND_URL=https://your-app.railway.app

# Socket.IO Configuration
# WebSocket connection for real-time messaging
VITE_SOCKET_URL=https://your-app.railway.app

# Application Environment
VITE_NODE_ENV=production

# API Configuration
VITE_API_TIMEOUT=10000
VITE_API_RETRY_ATTEMPTS=3

# Features Flags (if needed)
VITE_ENABLE_ANALYTICS=true
VITE_ENABLE_DEBUG=false

# Security Configuration
VITE_ENABLE_HTTPS_ONLY=true

# =============================================================================
# Instructions for Deployment:
# =============================================================================
# 1. Deploy backend to Railway first
# 2. Copy the Railway subdomain URL (e.g., https://my-app.railway.app)
# 3. Replace "https://your-app.railway.app" with your actual Railway URL
# 4. Deploy frontend to Vercel
# 5. Update backend .env FRONTEND_URL and ALLOWED_ORIGINS with Vercel URL
# 6. Redeploy backend with updated CORS settings
# =============================================================================